[package]
name = "vapor-library"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.11.0"

[dev-dependencies]
proptest = "1"
serde_json = "1"
