{
  "$comment": "The Vapor mark, pinned. Single source of truth for the app (src/components/VaporMark.tsx) and the icon exporter (scripts/export-mark.mjs), so a rendered icon and the running logo can never drift apart. Values are the ones the 'Ribbon Mark Lab' study renders at — the <vapor-ribbon> element's own fallbacks differ (turns 1.5, curl 0.85) and would quietly draw a different logo.",
  "pose": 0,
  "$pose": "Seconds into the idle animation. The renderer is deterministic, so this number IS the logo: same pose, same drawing, forever. 0 is the reference pose — the V with the hooked upper-left tip and the slightly elongated left arm. Legibility at 16px does not constrain this choice; the plate does that. Change it and re-run `npm run mark`, then `npx tauri icon brand/icon-1024.png`. `npm run mark -- --contact-sheet` renders the menu.",
  "geometry": {
    "turns": 1.6,
    "ribbon": 1,
    "curl": 0.55
  },
  "material": {
    "dispersion": 1,
    "hue": 0
  },
  "icon": {
    "$comment": "Icon plate. A glass mark has no intrinsic contrast — it is only visible by what it refracts — so with no plate it washes out on an unknown background. White is the default because a macOS icon sits in a row of light, shaded tiles and a dark slab reads as a hole in that row. iOS additionally forbids alpha in app icons, so a plate is required there.",
    "plate": "white",
    "inset": 0.14,
    "$inset": "Margin between plate edge and the mark's bounding box, as a fraction of the side.",
    "markTheme": "dark",
    "$markTheme": "Which environment the glass refracts, NOT the background it sits on. The dark environment renders a deeper, more saturated ribbon, which is what holds contrast against a white plate; the light environment refracts near-white and disappears into it.",
    "layerInset": 0.15,
    "$layerInset": "Margin for the Icon Composer foreground layer. Larger than the flat-icon inset because the system adds its own shadow and specular outside the artwork, and a layer that reaches the mask edge loses them.",
    "shadowOpacity": 0.5,
    "$shadowOpacity": "Opacity of the shadow macOS 26 drops under the mark layer in the .icon asset.",
    "layerBoost": "saturate(1.65) contrast(1.35) brightness(0.85)",
    "$layerBoost": "CSS filter applied to the Icon Composer layer only. The mark's shading assumes an environment and reads light on a white tile; this restores contrast without changing its geometry.",
    "appearances": {
      "$comment": "Per-appearance fills for the macOS 26 layered icon. Default keeps its white tile. Dark gets the brand near-black. Tinted gets a NEUTRAL GREY on purpose: for the tinted appearance you author a greyscale base and the system colourises it with whatever tint the user has chosen, so any colour cast here would skew every tint. Grey keeps it faithful.",
      "dark": "srgb:0.07100,0.07500,0.12200,1.00000",
      "tinted": "srgb:0.20000,0.20000,0.20000,1.00000"
    },
    "layerBoostLight": "saturate(0.55) brightness(1.55) contrast(0.95)",
    "$layerBoostLight": "Boost for the layer used by the Dark and Tinted appearances, which sit on a dark base and need a light mark."
  }
}
