// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.

export type TrackDetails = { href: string, title: string, artist: string, album: string, year: number, genre: string, 
/**
 * Absent until the track has been analysed, so the screen can say so
 * rather than print a column of zeroes.
 */
analysed: boolean, bpm: number, bpmIsManual: boolean, key: string, lufs: number, duration: number, cueIn: number, cueOut: number, energy: number, beats: number, waveform: Array<number>, 
/**
 * Where the file is, and whether it is here — the sovereignty claim, per
 * track.
 */
hrefPath: string, cached: boolean, 
/**
 * Why this track cannot be played, when it cannot (TD-12).
 */
unplayable: string | null, cover: string | null, 
/**
 * The file's own comment field — the closest thing to the design's
 * written notes that a file actually carries (TD-41b).
 */
notes: string | null, 
/**
 * True when any of this came from the file's tags rather than its path,
 * so the screen can stop claiming everything was derived from the name.
 */
tagged: boolean, 
/**
 * Which of these three the owner typed rather than the app deriving.
 *
 * The screen needs to distinguish "this is what we worked out" from "this
 * is what you told us" — the first invites a correction, the second offers
 * to undo one. Three flags rather than one, because a person who fixed a
 * genre has not thereby vouched for the album.
 */
genreIsManual: boolean, artistIsManual: boolean, albumIsManual: boolean, };
